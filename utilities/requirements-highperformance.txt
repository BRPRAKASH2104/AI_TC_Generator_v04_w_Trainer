# High-Performance AI Test Case Generator Dependencies
# Install with: pip install -r utilities/requirements-highperformance.txt

# Core dependencies (updated versions)
pandas>=2.3.2,<2.4.0                   # Updated for security patches
requests>=2.32.3,<2.33.0               # Updated for security patches (CVE fixes)
PyYAML>=6.0.2,<6.1.0
click>=8.2.0,<8.3.0
rich>=13.9.4,<14.0.0                   # Updated for Python 3.13.7 optimizations
openpyxl>=3.1.5,<3.2.0

# High-performance async and concurrency
aiohttp>=3.12.15,<4.0.0                # CRITICAL UPDATE: Security patches (CVE fixes)
asyncio-throttle>=1.0.2,<2.0.0         # Rate limiting for async operations

# Performance monitoring and system utilities
psutil>=6.1.0,<7.0.0                   # System resource monitoring (CPU, memory, disk)

# Optional performance accelerators (install if available)
# Uncomment these lines to enable maximum performance:

# XML Processing Acceleration (30-50% faster XML parsing)
lxml>=5.3.2,<6.0.0                     # Updated for security patches

# JSON Processing Acceleration (2-5x faster JSON operations)
ujson>=5.10.1,<6.0.0                   # Updated for Python 3.13.7 compatibility

# Memory Optimization
pympler>=0.9,<1.0                      # Memory profiling and optimization

# Development and debugging tools (optional)
memory-profiler>=0.61.0,<1.0.0        # Memory usage profiling
py-spy>=0.3.14,<1.0.0                 # Performance profiling tool
ruff>=0.8.0,<1.0.0                    # Fast linting and formatting (Rust-based)

# GPU acceleration libraries (optional, for future GPU features)
# Uncomment if GPU acceleration is needed:
# cupy-cuda12x>=13.0.0,<14.0.0         # GPU arrays and computing
# numba>=0.60.0,<1.0.0                 # JIT compilation for numerical functions

# Additional performance monitoring (optional)
line_profiler>=4.1.0,<5.0.0           # Line-by-line performance profiling