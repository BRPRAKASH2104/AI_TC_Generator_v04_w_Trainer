# ⚠️  DEPRECATED - This file is no longer maintained
#
# This project uses pyproject.toml as the single source of truth for dependencies.
# Please install using one of the following methods:
#
# For production use:
#   pip install -e .
#
# For development (includes dev tools like pytest, ruff, mypy):
#   pip install -e .[dev]
#
# For training features (includes torch, transformers):
#   pip install -e .[training]
#
# For all dependencies:
#   pip install -e .[all]
#
# See pyproject.toml for the complete dependency specification.
#
# Python 3.14+ required (no backward compatibility)

# Legacy compatibility (minimal dependencies for basic usage)
# If you must use requirements.txt, install from pyproject.toml instead:
#   pip install -e .

pandas>=2.2.3,<3.0.0
pydantic>=2.10.4,<3.0.0
pydantic-settings>=2.7.0,<3.0.0
requests>=2.32.3,<3.0.0
PyYAML>=6.0.2,<7.0.0
click>=8.1.8,<9.0.0
rich>=13.9.4,<14.0.0
openpyxl>=3.1.5,<4.0.0
aiohttp>=3.12.15,<4.0.0
